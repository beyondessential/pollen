# `openapi-typescript` peer-pins TypeScript ^5 but we use TS 6. It's a CLI that
# ships its own bundled TS, so the peer-dep is advisory and the mismatch doesn't
# bite at runtime. Centralising the opt-in here keeps CI, build.rs, and manual
# `npm install` consistent.
legacy-peer-deps=true
